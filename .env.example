# PerFi Delta - Environment Variables Template
# Copia este archivo a .env y completa los valores

# Resend API Key - https://resend.com/api-keys
# En dev: si no la seteas, los mails van a /dev/mailbox (no a Resend).
RESEND_API_KEY=re_tu_api_key_aqui

# Email remitente - Cambiar después de verificar dominio en Resend
FROM_EMAIL=onboarding@resend.dev

# Database
DB_HOSTNAME=db
DB_PASSWORD=postgres

# Server
PORT=4000
